import 'package:flutter/material.dart';
import '../layout/master_layout.dart';

class uaboutPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MasterLayout(
      child: Center(
        child: Text('about_page placeholder'),
      ),
    );
  }
}

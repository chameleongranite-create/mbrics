import 'package:flutter/material.dart';

/// Brand palette constants for mBrics
const Color platinumFill = Color(0xFFE5E4E2);   // Core function cards
const Color backgroundWhite = Color(0xFFFFFFFF); // Utility cards + backgrounds
const Color gold = Color(0xFFC2994B);            // Primary buttons, utility icons
const Color silver = Color(0xFFA7A9AC);          // Secondary accents, core icons
const Color textDark = Color(0xFF343A40);        // Headings + card titles
const Color borderGray = Color(0xFFE0E0E0);      // Card borders
const Color cardTint = Color(0xFFFAFAFA);        // Light card background
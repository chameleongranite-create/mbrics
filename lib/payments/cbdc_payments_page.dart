import 'package:flutter/material.dart';
import '../layout/master_layout.dart';

class ucbdc_paymentsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MasterLayout(
      child: Center(
        child: Text('cbdc_payments_page placeholder'),
      ),
    );
  }
}

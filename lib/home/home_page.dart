import 'package:flutter/material.dart';
import '../layout/master_layout.dart';

class uhomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MasterLayout(
      child: Center(
        child: Text('home_page placeholder'),
      ),
    );
  }
}

import 'package:flutter/material.dart';
import '../layout/master_layout.dart';

class upartnersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MasterLayout(
      child: Center(
        child: Text('partners_page placeholder'),
      ),
    );
  }
}

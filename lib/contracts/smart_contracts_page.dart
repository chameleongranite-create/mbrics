import 'package:flutter/material.dart';
import '../layout/master_layout.dart';

class usmart_contractsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MasterLayout(
      child: Center(
        child: Text('smart_contracts_page placeholder'),
      ),
    );
  }
}

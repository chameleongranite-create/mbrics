import 'package:flutter/material.dart';
import '../layout/master_layout.dart';

class utrust_securityPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MasterLayout(
      child: Center(
        child: Text('trust_security_page placeholder'),
      ),
    );
  }
}

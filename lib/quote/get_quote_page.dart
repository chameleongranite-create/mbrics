import 'package:flutter/material.dart';
import '../layout/master_layout.dart';

class uget_quotePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MasterLayout(
      child: Center(
        child: Text('get_quote_page placeholder'),
      ),
    );
  }
}
